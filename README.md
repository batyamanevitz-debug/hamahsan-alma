# ALMA / המחסן — דף הבית

מימוש פיקסל-פרפקט ורספונסיבי של עיצוב הפיגמה `עיצוב אתר סחר`
(רוחב עיצוב 1920px):

* **דף הבית** — node `631:2441` → [`index.html`](index.html)
* **דף מוצר** — node `577:24` → [`product.html`](product.html)
  (מקושר מכרטיס המוצר *Perfumes De Marly* ומעיגול המותג בדף הבית)
* **דף תשלום** — node `577:771` → [`checkout.html`](checkout.html)
  (מקושר מכפתור *קנה עכשיו* בדף המוצר)
* **דפי קטגוריה** — node `577:1952`, תבנית אחת ב־[`category.html`](category.html)
  לפי `?cat=`. ארבע קטגוריות ראשיות (בשמי נשים כברירת מחדל, `perfume`,
  `makeup`, `hair`) ו‑15 תתי קטגוריות שמגיעות מהתפריטים הנפתחים —
  `brands`, `unisex`, `men`, `luxury`, `sets`, `kids`, `best`,
  `beauty-brands`, `cosmetics`, `body`, `face`, `hair-brands`,
  `devices`, `styling`, `haircare`.
* **תפריטים נפתחים** בהדר — nodes `679:138` (בשמים), `679:286` (איפור),
  `679:277` (הכל לשיער). קיימים בכל הדפים.
* **פאנל חיפוש** — node `593:327`. נפתח מאייקון הזכוכית המגדלת בהדר,
  בכל הדפים.
* **סל קניות (מגירה)** — node `582:103`. נפתח מכפתורי "הוספה לסל"
  (דף מוצר, סרגל דביק, כרטיסי הקטגוריה) ומאייקון העגלה בהדר. קיים בכל הדפים.
* **חלוניות הסינון והמיון** בדף הקטגוריה — nodes `618:135` (מותג),
  `619:510` (מחיר), `618:254` (דירוג), `619:484` (מרכיב),
  `619:427` (סגנון הניחוח), `619:458` (צורך), `619:571` (מיון לפי).

## הרצה

```bash
node server.js
```

ואז לפתוח http://localhost:4321
(אפשר גם לפתוח את `index.html` ישירות בדפדפן).

## אינטראקטיביות (מה באמת עובד)

האתר סטטי אבל מתפקד — כל המצב נשמר ב־localStorage ומשותף בין כל הדפים:

| יכולת | פרטים |
|---|---|
| **עגלת קניות** | הוספה מדף מוצר, מהסרגל הדביק ומכרטיסי הקטגוריה. המגירה נטענת מהמצב: שינוי כמות, מחיקה, סיכום ביניים ומונה בהדר. נשמרת בין דפים ורענונים. |
| **מועדפים** | הלב בדף המוצר מוסיף/מסיר, המונה בהדר מתעדכן והמצב נשמר. |
| **חיפוש** | הקלדה מציגה תוצאות חיות מקטלוג של 33 מוצרים (עברית ואנגלית) עם תמונה ומחיר. Enter מוביל למוצר הראשון. |
| **קטגוריות** | 19 תצוגות מאותה תבנית — ארבע ראשיות ו‑15 תתי קטגוריות. כל מוצר מתויג במפורש לתתי הקטגוריות שלו. הבאנר, הכותרת, עיגולי הניווט, רשימת המותגים וטווח המחירים נבנים מהמוצרים שבקטגוריה. |
| **קטלוג מוצרים** | נטען מטבלת `products` ב-Supabase בכל טעינת דף. עריכת מוצר או מחיר בדשבורד משנה את האתר בלי לגעת בקוד. אם הדאטהבייס לא זמין — נעשה שימוש בעותק שמור, ואם גם הוא חסר בקטלוג המוטמע ב-`store.js`. |
| **כל הקטגוריות** | `categories.html` — עץ הקטגוריות המלא: כל קטגוריה ראשית עם תתי הקטגוריות שלה ומספר המוצרים בכל אחת. הכפתור ״לכל הקטגוריות״ בעמוד הבית מוביל לשם. |
| **מבצעים / חדש / מותגים** | `category.html?cat=sale` מציג כל מוצר שיש לו מחיר לפני הנחה. `category.html?cat=new` מציג מוצרים שסומנו ״מוצר חדש״ בלוח. `brands.html` הוא אינדקס המותגים בעיצוב של רצועת המותגים בעמוד הבית — עיגול לילך, לוגו ושם. מותג בלי לוגו מקבל עיגול עם ראשי התיבות שלו. כל מותג מוביל ל-`category.html?brand=<slug>`, ורצועת המותגים בעמוד הבית מקשרת לאותם דפים. |
| **קטגוריות מהלוח** | עץ הקטגוריות נטען מטבלת `categories`. שינוי שם, העברה לקטגוריית אב אחרת, החלפת תמונה, הוספה או מחיקה — כולם משפיעים על האתר: כותרות, פירורי לחם, עיגולי הניווט וכתובות הדפים. |
| **הגדרות מהלוח** | רצועת ההכרזה ופרטי הקשר בפוטר נטענים מ-`site_settings` בכל דף. עריכה בלוח משנה את כל האתר. |
| **דפי תוכן ובלוג** | `page.html?slug=` לדף תוכן, `page.html?post=` למאמר, `page.html?blog` לרשימת המאמרים. סקשן הבלוג בעמוד הבית וקישורי הפוטר נבנים מהדאטהבייס. |
| **הזמנות** | דף התשלום שומר הזמנה אמיתית דרך `place_order`, והיא מופיעה מיד בלשונית ההזמנות בלוח. |
| **פירורי לחם** | היררכיה אמיתית: ראשי › קטגוריה ראשית › תת קטגוריה › המוצר. כל פירור מקשר לדף שבאמת מכיל את המוצר. תת קטגוריה שנקראת כמו הראשית מתקפלת לפירור אחד. |
| **סינון בקטגוריה** | מותג, מרכיב, סגנון, צורך, דירוג וטווח מחיר — מסננים בפועל; מספר הפריטים והתווית על הצ'יפ מתעדכנים. |
| **מיון** | שם א-ת, שם ת-א, מחיר עולה, מחיר יורד — ממיין את הרשת בפועל. |
| **דפדוף** | 8 מוצרים בעמוד; מספר העמודים מתעדכן לפי הסינון. |
| **דף מוצר דינמי** | `product.html?id=<מזהה>` בונה את הדף מהקטלוג — 33 מוצרים, לכל אחד שם, מק"ט, מחיר, גלריה, תיאורים, מוצרים דומים והוספה לסל. |
| **דף מוצר** | גלריה (גם בחצי מקלדת), טאבים, חוות דעת עם 3 עמודים אמיתיים, סינון 5 כוכבים והצבעות "מועיל". |
| **דף תשלום** | סיכום ההזמנה נבנה מהעגלה, מע"מ 18%, קופונים, ולידציה עם הודעות שגיאה, ומסך אישור הזמנה שמנקה את הסל. |
| **ניוזלטר** | ולידציית מייל ואישור. |
| **קישורים** | תפריטים, כרטיסי מוצר, מותגים וכפתורי ההירו מובילים לדפים אמיתיים. קישור שאין לו דף בעיצוב מציג הודעה במקום לקפוץ לראש העמוד. |
| **ווטסאפ** | בועת "דברו איתנו" פותחת שיחה אמיתית ב־wa.me. |

קודי קופון לבדיקה: `ALMA10` (10%), `ALMA20` (20%), `WELCOME` (15%).

## לוח ניהול

[`admin.html`](admin.html) — ניהול מלא של החנות מהדפדפן, בעברית ורספונסיבי.
שש לשוניות: מוצרים, קטגוריות ומותגים, דפי תוכן, בלוג, הגדרות חנות והזמנות.

### כניסה

דוא״ל וסיסמה של משתמש Supabase שרשום בטבלת `admins`.
הסיסמה אינה נמצאת בקוד — היא נבדקת מול Supabase Auth.

במסך יש גם קישור **״שכחתי סיסמה״** ששולח קישור כניסה חד-פעמי למייל.
זה גיבוי בלבד: השירות המובנה של סופאבייס מוגבל לכמה מיילים בשעה,
ולכן הכניסה הרגילה היא עם סיסמה.

**הוספת מנהל:**

1. Supabase → Authentication → Users → **Add user** → **Create new user**
   (עם סיסמה ועם Auto Confirm User מסומן).
2. בעורך ה-SQL: `select public.grant_admin('המייל@שלו.com');`

שימו לב: מחיקת משתמש ויצירתו מחדש יוצרת מזהה חדש, ולכן צריך
להריץ שוב את `grant_admin` — הרשומה הישנה נמחקת יחד עם המשתמש.

חשוב: משתמש מחובר **אינו** מנהל. רק מי שרשום בטבלת `admins` יכול לשנות
משהו — כל פוליסת כתיבה בדאטהבייס דורשת `public.is_admin()`. לכן העובדה
ש-`admin.html` נגיש לכולם ב-GitHub Pages אינה חושפת את החנות.

### מה אפשר לעשות שם

| לשונית | פעולות |
|---|---|
| **מוצרים** | הוספה, עריכה ומחיקה. חיפוש, סינון לפי קטגוריה ולפי מלאי. העלאת תמונה ראשית וגלריה, מחיר מבצע, שיוך למותג ולקטגוריות, תיאורים ועורך טקסט עשיר. |
| **קטגוריות ומותגים** | עץ קטגוריות ראשיות ותתי קטגוריות, תמונות, לוגואים של מותגים, ומספר המוצרים בכל אחת. |
| **דפי תוכן** | אודות, תקנון, משלוחים ושאלות נפוצות — עריכה עם עורך טקסט או HTML גולמי, וסימון פרסום. |
| **בלוג** | מאמרים עם תמונת נושא, תקציר, מחבר וסטטוס פרסום. |
| **הגדרות חנות** | 23 הגדרות בשש קבוצות: מסך הפתיחה (כותרות, תמונה, שני כפתורים), רצועת ההכרזה, חמשת יתרונות החנות, טקסטי הניוזלטר, פרטי קשר ומספר וואטסאפ, ומע״מ/דמי משלוח/סף משלוח חינם. |
| **קודי הנחה** | הוספה, עריכה ומחיקה של קופונים, אחוז הנחה, תאריך תפוגה והפעלה/כיבוי. |
| **חוות דעת** | ההמלצות שמופיעות בעמוד הבית — טקסט, שם, דירוג, סדר והצגה. |
| **הזמנות** | רשימה עם חיפוש וסינון, פירוט הפריטים, ועדכון סטטוס (ממתין / בטיפול / נשלח / הושלם / בוטל). |

## דאטהבייס (Supabase)

הפרויקט הוא `touuyegybctmfdtzlbmt` (region ap-southeast-2). הטבלאות:

| טבלה | תפקיד |
|---|---|
| `products` | הקטלוג. `sort_order` קובע את סדר ההצגה. |
| `categories` | עץ הקטגוריות. `parent_id` ריק = קטגוריה ראשית. ה-`slug` הוא המפתח ב-`category.html?cat=` |
| `brands` | מותגים ולוגואים. `products.brand_id` מקשר אליהם |
| `pages` | דפי תוכן סטטיים |
| `blog_posts` | מאמרי הבלוג |
| `site_settings` | מפתח/ערך: באנרים, כותרות ופרטי קשר |
| `orders` | הזמנות. `items` ו-`shipping_address` הם JSON |
| `coupons` | קודי הנחה לדף התשלום |
| `testimonials` | חוות דעת בעמוד הבית |
| `admins` | מי רשאי לנהל את החנות |

התאמת מוצר לקטגוריה נעשית לפי `category_id` ולא לפי שם, כדי ששינוי שם
לא ינתק מוצרים. `products.cat` ו-`products.brand` הם עותקים לתצוגה
ושני טריגרים מעדכנים אותם גם כששם הקטגוריה או המותג משתנה.

ההזמנות נכתבות דרך `place_order` ולא בהוספה ישירה: כך דף התשלום מקבל
את מספר ההזמנה בלי שתהיה לו הרשאת קריאה לטבלה. הפונקציה מוודאת שם,
מייל תקין, סל לא ריק וסכום סביר.

ב-`products` השדות `brand` ו-`cat` הם עותקים לתצוגה שמסונכרנים אוטומטית
מ-`brand_id` ומ-`category_id` על ידי טריגר, כדי שהחנות תוכל לשלוף שורה
שטוחה אחת בלי join.

* **קריאה** — האתר פונה ל-REST של Supabase עם מפתח ציבורי (`sb_publishable_...`).
  המפתח נמצא ב-`js/store.js` בכוונה: RLS על הטבלה מתיר `select` בלבד,
  ולכן אי אפשר להוסיף, לשנות או למחוק מוצרים איתו.
* **כתיבה** — רק מהדשבורד של Supabase או עם service key. אין לאתר הרשאת כתיבה.
* **עריכה** — לשנות מחיר או שם בטבלה, לרענן את הדף, וזהו. אין צורך בקומיט.
* **סדר** — לשנות `sort_order` כדי להזיז מוצר קדימה או אחורה ברשת.

### שלוש שכבות של גיבוי

פרויקט Supabase חינמי נכנס להשהיה אחרי כשבוע בלי שימוש, ולכן `js/store.js`
לא סומך על הדאטהבייס בלבד:

1. **Supabase** — המקור הרגיל.
2. **עותק שמור** ב-localStorage (`alma.catalog.v1`) — אם הרשת נכשלת או שהתשובה
   מאחרת יותר מ-5 שניות.
3. **הקטלוג המוטמע** ב-`store.js` — אם גם עותק שמור אין. האתר תמיד מציג מוצרים.

`Store.source` מדווח מאיפה הגיע הקטלוג בפועל (`supabase` / `cache` / `fallback`),
ו-`Store.ready` הוא ה-promise שדפי הקטגוריה והמוצר ממתינים לו לפני שהם מציירים.

## מבנה

```
index.html        דף הבית — HTML סמנטי, RTL
product.html      דף מוצר (משתמש באותם הדר/ניוזלטר/פוטר)
checkout.html     דף תשלום
category.html     דף קטגוריה — תבנית לכל הקטגוריות לפי ?cat=
css/style.css     בסיס, טוקנים, דף הבית, הדר + התפריטים הנפתחים
css/product.css   דף המוצר בלבד (נטען אחרי style.css)
css/checkout.css  דף התשלום בלבד (נטען אחרי style.css)
css/category.css  דף הקטגוריה בלבד (נטען אחרי style.css)
js/store.js       עגלה + מועדפים + קטלוג (Supabase + localStorage) — נטען ראשון
js/main.js        תפריט, מגירת עגלה, חיפוש, מועדפים, קרוסלות, ניוזלטר
js/product.js     גלריה, טאבים, הוספה לסל, חוות דעת, סרגל דביק
js/checkout.js    סיכום מהעגלה, קופון, ולידציה, אישור הזמנה
js/category.js    סינון, מיון, דפדוף והוספה לסל
js/page.js        דפי תוכן, מאמרים ורשימת הבלוג
js/brands.js      אינדקס המותגים
js/cats.js        עץ כל הקטגוריות
categories.html   דף כל הקטגוריות
css/cats.css      דף הקטגוריות בלבד (נטען אחרי style.css)
brands.html       דף המותגים
css/brands.css    דף המותגים בלבד (נטען אחרי style.css)
page.html         תבנית לדפי תוכן ולמאמרי הבלוג
css/page.css      דפי התוכן בלבד (נטען אחרי style.css)
assets/           כל התמונות והאייקונים שיוצאו מהפיגמה
server.js         שרת סטטי קטן לתצוגה מקומית
```

## טוקנים מהעיצוב

| שם | ערך |
|---|---|
| צבע ראשי (בורדו/סגול) | `#7D415F` |
| סגול לילך | `#CBB1E0` |
| סגול כותרות | `#B48FC2` |
| טקסט כהה | `#110D0A` / `#222222` |
| טקסט אפור | `#757575` / `#5A5A5A` |
| רקע פוטר | `#FBFBFB` |
| פונטים | Assistant (עברית) + Jost (לטיני) |

## נקודות שבירה

`1600` · `1440` · `1280` · `1100` · `1024` (מעבר להמבורגר) · `768` · `480`

## הערות

* המרווחים והגדלים בדסקטופ נלקחו ישירות מהפיגמה (רוחב תוכן 1280px,
  כותרות 40px/55, כרטיס מוצר 296.25px, קלף המלצה 405×237 וכו').
* סדר הפריטים בשורות (תפריט, קטגוריות, מוצרים, מותגים, פוטר) מותאם
  לסדר הפיזי שבעיצוב — חלק מהשורות מסודרות LTR למרות שהאתר RTL,
  בדיוק כמו בקובץ המקור.
* אייקון האינסטגרם ברצועת הסושיאל יוצא ריק מהפיגמה, ולכן שוחזר כ‑SVG
  זהה בגודל ובצבע.
* כותרת המאמר הראשי בבלוג מוצגת במלואה
  ("מהפכת ה‑K‑Beauty בישראל…"), בעוד שבפיגמה היא חתוכה בקצה המסגרת.
* בדף המוצר: סרגל ה"הוספה לסל" הדביק מופיע אחרי גלילה מעבר לכפתורי
  הרכישה ונעלם מעל הפוטר (בפיגמה הוא מצויר כשכבה סטטית).
* הטאבים "למי זה מתאים" ו"למה תתאהבי בו" מקבלים טקסט קצר משלהם —
  בפיגמה עוצב רק תוכן הטאב "על המוצר".
* התפריטים הנפתחים: בדסקטופ נפתחים ב‑hover ובלחיצה (וגם מקלדת),
  ונסגרים ב‑Esc / לחיצה בחוץ. במובייל הם נפתחים כאקורדיון בתוך תפריט
  ההמבורגר, בלי התמונה. לתפריט "הכל לשיער" אין תמונה גם בפיגמה,
  ולכן הפאנל שלו ברוחב התוכן בלבד.
* באנר ההירו בעמוד הבית הוא תמונה ביחס 2:1 (1920x937). בדסקטופ הוא
  משמש רקע והטקסט מונח עליו, כמו בפיגמה. מתחת ל-1024px יחס כזה לא
  יכול למלא גובה קבוע בלי לחתוך את הבקבוק ואת שם המותג, ולכן התמונה
  עוברת לרצועה מלאה ביחס המקורי והטקסט יורד מתחתיה על רקע תואם.
* בדף הקטגוריה: רשת המוצרים זורמת LTR (כמו בפיגמה — המחירים יורדים
  משמאל לימין), בעוד שתוכן הכרטיסים עצמם RTL.
* חלוניות הסינון נפתחות בלחיצה ונסגרות ב‑Esc / לחיצה בחוץ, והן מסננות
  וממיינות את הרשת בפועל. במסכים צרים החלונית נצמדת אוטומטית פנימה
  כדי לא לגלוש מהמסך.
* פאנל החיפוש נפתח מתחת להדר בגובה 119px, עם קו תחתון לשדה, אייקון
  חיפוש בימין וכפתור סגירה מעליו, ומציג תוצאות חיות מהקטלוג.
* סל הקניות נפתח כמגירה מצד שמאל (שם נמצא אייקון העגלה) עם שכבת כהות
  מאחוריו, ותוכנו נטען מהמצב השמור — כך שהוא זהה בכל הדפים וגם אחרי רענון.
* בדף התשלום: יש ולידציה, קופונים ומסך אישור הזמנה — אבל אין שליחה
  לשרת ואין סליקה אמיתית.
  שם המוצר בסיכום ההזמנה מופיע בשחור (בפיגמה הוא לבן על רקע לבן,
  כלומר בלתי נראה).
